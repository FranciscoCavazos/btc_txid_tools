Put your spreadsheet here, e.g.:
- transactions.csv  (preferred)
- transactions.xlsx (sheet name configurable in the notebook)

Expected columns:
- txid (string, Bitcoin transaction ID)
- from (optional)
- to (optional)
- amount (optional)

Only `txid` is required for enrichment.
